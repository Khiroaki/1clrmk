&НаСервере
Процедура ВойтиНаСервере()
	ПолучитьПользователя = Справочники.Пользователи.НайтиПоНаименованию(Логин);
	Если Логин = ПолучитьПользователя.Наименование Тогда
		Если Пароль = ПолучитьПользователя.Пароль Тогда
			Сообщить("Вы вошли как " + Логин);
				Константы.ФИО.Установить(ПолучитьПользователя.ФИО);
				Константы.Логин.Установить(ПолучитьПользователя.Наименование);
				Константы.Номер.Установить(ПолучитьПользователя.Номер);
				Константы.Почта.Установить(ПолучитьПользователя.Почта);
		Иначе
			Сообщить("Неверный логин или пароль");
		КонецЕсли;
	Иначе
		Сообщить("Неверный логин или пароль");
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура Войти(Команда)
	ВойтиНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура Регистрация(Команда)
	ПолучитьФорму("ОбщаяФорма.Регистрация").Открыть();	
КонецПроцедуры
