Процедура ПередНачаломРаботыСистемы(Отказ)
	
	Если Не ПолныеПрава.РольДоступнаПользователю("Администратор") Тогда
		Если Не ПолныеПрава.ПараметрСеансаУстановлен("ТекущийПользователь") Тогда
			Предупреждение("Пользователь: " + ИмяПользователя() + " не найден в справочнике!");
			Отказ = Истина;
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ПолныеПрава.ОбработкаВходаПользователя("Вошёл");
	
КонецПроцедуры

Процедура ПередЗавершениемРаботыСистемы(Отказ, ТекстПредупреждения)
	// ПолныеПрава.ОбработкаВходаПользователя("Вышел");
КонецПроцедуры
